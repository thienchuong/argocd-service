apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: dev
spec:
  rules:
    - host: frontend.chuong.click
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  name: http
  ingressClassName: ingress-nginx
  
for i in $(seq 1 10); do curl -s --resolve frontend.chuong.click:443:52.74.87.207 frontend.chuong.click; done
